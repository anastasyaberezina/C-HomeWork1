//Не могу решить эту задачу. Не понимаю, куда писать условие N>X>=1.

Console.Write("Введите число: ");
int N = int.Parse(Console.ReadLine());
int X = 0;

while (X>=1);
while (X<N)
if ((N+X)%2 == 0)
{
 X = N/2;
 Console.Write($"{N} + ","");
}
X++;

